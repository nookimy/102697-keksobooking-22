const LOCATION_DECIMAL = 5;
const MIN_ADS_COUNT = 0;
const MAX_ADS_COUNT = 10;
const MIN_PRICES = {
  bungalow: 0,
  flat: 1000,
  house: 5000,
  palace: 10000,
};

export {
  LOCATION_DECIMAL,
  MIN_PRICES,
  MIN_ADS_COUNT,
  MAX_ADS_COUNT
};
